{
  "name": "registryapi",
  "scripts": {
    "format": "prettier --write",
    "prepare": "husky",
    "pre-commit": "lint-staged --allow-empty"
  },
  "lint-staged": {
    "src/**/*.java": ["pnpm format"]
  },
  "devDependencies": {
    "@commitlint/config-conventional": "^19.2.2",
    "commitlint": "^19.3.0",
    "cz-conventional-changelog": "^3.3.0",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.7",
    "prettier": "^3.2.5",
    "prettier-plugin-java": "^2.6.0"
  }
}
